<!DOCTYPE node PUBLIC "-//freedesktop//DTD D-BUS Object Introspection 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/introspect.dtd">
<node>
  <!--
    pl.jkolo.yubikey.oath.Manager:
    @short_description: Manager interface for YubiKey OATH daemon

    This interface provides version information for the daemon.
    Device and credential discovery is handled by the ObjectManager interface.

    Following D-Bus best practices, this interface does NOT provide aggregated
    properties (DeviceCount, Devices, TotalCredentials, Credentials).
    Clients should use GetManagedObjects() from org.freedesktop.DBus.ObjectManager
    to discover the complete hierarchy and calculate aggregates locally.
  -->
  <interface name="pl.jkolo.yubikey.oath.Manager">

    <!-- Properties -->
    <property name="Version" type="s" access="read">
      <annotation name="org.freedesktop.DBus.Property.EmitsChangedSignal" value="const"/>
    </property>

  </interface>

  <!-- Note: Standard D-Bus interfaces (ObjectManager, Properties, Introspectable, Peer)
       are handled automatically by Qt's meta-object system and do not need XML definitions
       for adaptor generation. They are implemented in the C++ class.

       The ObjectManager interface provides:
       - GetManagedObjects() method for discovering all devices and credentials
       - InterfacesAdded / InterfacesRemoved signals for tracking hierarchy changes
  -->

</node>
